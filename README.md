# see-arduino-auto-miner

run miner:
```
python -m venv venv
source venv/bin/activate  # MacOS/Linux
venv\Scripts\activate  # Windows
pip install -r requirements.txt
py main.py
```

if you stop, reconnect the arduino